## 1. 接口修正与补充报告 (API Revision and Supplement Report)

**针对问题接口/需求的修正建议 (Corrections for existing APIs):**

| 原始需求ID/描述 | 问题描述 | 修正后的接口片段 |
|---|---|---|
| REQ-001 / 品牌与欢迎语 | 后端接口已定义 `logoText` 与 `welcomeText`，但未纳入可验证的验收标准；后端测试未覆盖。参见 `Home_Page/.artifacts/api_interface.yml:11-13` 与 `:24-25` | ```yaml
id: API-GET-HomeMetadata
output:
  success:
    statusCode: 200
    body:
      logoText: string # 例如: "12306"
      welcomeText: string # 例如: "欢迎登录12306"
  error:
    - statusCode: 500
      body: { code: "server_error", message: "Server error.", requestId: string }
acceptanceCriteria:
  - "返回体包含 logoText 与 welcomeText，均为非空字符串。"
``` |
| REQ-001 / 登录注册跳转（标题与面包屑） | UI 接口缺少对“面包屑/页面标题”的状态定义；前端测试未校验。参见 `Home_Page/.artifacts/ui_interface.yml:27-39` | ```yaml
id: UI-HomePage
state:
  - breadcrumbs # string[]，例如 ["首页", "登录"]
  - pageTitle # string，导航到 /auth 后应为 "登录/注册"
acceptanceCriteria:
  - "点击‘登录注册’后，展示正确 pageTitle 与 breadcrumbs。"
``` |
| REQ-001 / 登录态拦截与提示 | UI 状态 `loginStatus` 未明确定义为布尔，并且测试对未导航的约束不完整。参见 `Home_Page/.artifacts/ui_interface.yml:15-22` 与 `frontend/test/components/HomePage.test.tsx:59-67` | ```yaml
id: UI-HomePage
properties:
  - name: loginStatus
    type: boolean
    description: "当前是否已登录。"
acceptanceCriteria:
  - "loginStatus=false 时，点击管理类按钮仅触发 onLoginRequired，不触发 onNavigate。"
``` |
| REQ-001 / 无障碍键盘导航 | 接口与测试未验证 Tab 实际焦点顺序与 Enter/Space 触发。参见 `frontend/test/components/HomePage.test.tsx:85-91` | ```yaml
id: UI-HomePage
properties:
  - name: focusOrder
    type: string[]
    description: "按钮焦点顺序，默认 ['登录注册','车票查询','订单管理','乘客管理','个人信息']"
acceptanceCriteria:
  - "使用键盘 Tab 时焦点顺序严格符合 focusOrder；Enter/Space 可触发。"
``` |
| REQ-001 / 响应式布局与触控尺寸 | 测试未验证视口断点与按钮触控尺寸≥44px。参见 `frontend/test/components/HomePage.test.tsx:95-99` | ```yaml
id: UI-HomePage
properties:
  - name: breakpoints
    type: { mobile: 768, tablet: 1200 }
    description: "布局断点"
acceptanceCriteria:
  - "≤768px 视口下主内容纵向堆叠；按钮的最小触控高度≥44px。"
``` |
| REQ-001 / 错误提示与日志（不含敏感信息） | 后端接口仅定义 500，无结构化错误码与请求关联标识；前端未验证日志脱敏。参见 `Home_Page/.artifacts/api_interface.yml:24-25` 与 `frontend/test/components/HomePage.test.tsx:112-119` | ```yaml
id: API-GET-HomeMetadata
output:
  error:
    - statusCode: 429
      body: { code: "rate_limit", message: "Too Many Requests.", requestId: string }
    - statusCode: 503
      body: { code: "service_unavailable", message: "Service temporarily unavailable.", requestId: string }
    - statusCode: 400
      body: { code: "invalid_request", message: string, requestId: string }
logging:
  clientErrorEvent:
    fields: [ "code", "message", "requestId", "ts", "page" ] # 排除PII
``` |

**补充的新接口定义 (New APIs to be added):**

```yaml
- type: Backend
  id: API-GET-SessionStatus
  route: GET /api/session/status
  description: "查询当前会话的登录状态，用于首页管理入口拦截与跳转判定。"
  input:
    type: None
  output:
    success:
      statusCode: 200
      headers:
        Cache-Control: "no-store"
      body:
        authenticated: boolean
        userId: string | null
        expiresAt: string # ISO8601
        roles:
          - string
    error:
      - statusCode: 503
        body: { code: "service_unavailable", message: string, requestId: string }
  acceptanceCriteria:
    - "成功返回 authenticated 布尔值；未登录时 userId=null。"
    - "headers 包含 no-store，避免缓存登录态。"

- type: Backend
  id: API-POST-ClientEventLog
  route: POST /api/logs/client-events
  description: "接收前端导航错误等事件日志，要求脱敏（不含账号、手机号、证件号等PII）。"
  input:
    type: application/json
    body:
      eventType: string # e.g., "navigation_error"
      code: string # e.g., "route_failed"
      message: string
      requestId: string
      page: string # e.g., "/"
      ts: string # ISO8601
    constraints:
      - "PII 禁止：不得包含姓名、手机号、证件号、邮件等。"
  output:
    success:
      statusCode: 202
      body: { accepted: true }
    error:
      - statusCode: 400
        body: { code: "invalid_payload", message: string, requestId: string }
  acceptanceCriteria:
    - "有效负载返回 202，并在服务端持久化或转发。"
    - "拒绝包含敏感信息的负载并返回 400。"
```

## 2. 测试用例修正与补充报告 (Test Case Revision and Supplement Report)

**针对现有用例的修正建议 (Corrections for existing Test Cases):**

| 接口名称 | 原用例ID/描述 | 问题描述 | 修正后的预期结果 |
|---|---|---|---|
| API-GET-HomeMetadata | `backend/test/routes/site.test.js:7-14` / 返回 200 包含服务时间与官方提示 | 未校验 `logoText` 与 `welcomeText` | `res.body.logoText` 与 `res.body.welcomeText` 为非空字符串，匹配品牌与欢迎语 |
| API-GET-HomeMetadata | `backend/test/routes/site.test.js:18-24` / 友情链接至少包含铁路官方 | 未校验 URL 合法与 HTTPS | `friendLinks[0].url` 为有效 `https://` 链接，且域名包含 `12306`/`kyfw.12306.cn` |
| API-GET-HomeMetadata | `backend/test/routes/site.test.js:28-34` / 合规信息包含公安与ICP备 | 未校验格式与非空 | `policeRecord` 与 `icpRecord` 均非空并匹配备案号格式（如 `ICP` 前缀） |
| API-GET-HomeMetadata | `backend/test/routes/site.test.js:38-44` / 无障碍入口说明 | 未校验入口路径或可访问名称 | `accessibility.elderlyServiceEntry` 为有效路径（如 `/accessibility`），`description` 非空 |
| UI-HomePage | `frontend/test/components/HomePage.test.tsx:39-45` / 登录注册跳转 | 未校验页面标题与面包屑 | 导航到 `/auth` 后，DOM 出现标题 `登录/注册` 与面包屑 `首页 / 登录` |
| UI-HomePage | `frontend/test/components/HomePage.test.tsx:59-67` / 未登录拦截 | 未断言不发生导航 | `onLoginRequired` 被调用且 `onNavigate` 未被调用；出现登录提示文案 |
| UI-HomePage | `frontend/test/components/HomePage.test.tsx:70-81` / 登录后管理跳转 | 未设置 `loginStatus=true` | 渲染时显式传入 `loginStatus=true`，分别调用 `onNavigate('/orders'|' /passengers'|' /profile')` |
| UI-HomePage | `frontend/test/components/HomePage.test.tsx:85-91` / 键盘导航 | 未验证焦点顺序与按键触发 | 使用 `userEvent.tab()` 验证焦点顺序；对每个按钮使用 `userEvent.keyboard('{Enter}')` 与 `'{Space}'` 触发 |
| UI-HomePage | `frontend/test/components/HomePage.test.tsx:95-99` / 响应式 | 未模拟视口 ≤768px 与触控尺寸 | 设置视口宽度 ≤768px，断言按钮 `min-height >= 44px` 与纵向堆叠样式 |
| UI-HomePage | `frontend/test/components/HomePage.test.tsx:103-108` / 页脚与合规信息 | 校验文案但未校验具体数据 | Mock API 返回并断言至少一个友情链接名称与有效链接；备案号具体数字存在 |
| UI-HomePage | `frontend/test/components/HomePage.test.tsx:112-119` / 错误提示与操作 | 未校验日志脱敏与事件上报 | 触发 `onError` 后断言调用事件上报（不含姓名/手机号/证件号等）且包含 `requestId` 与 `code` |

**补充的新增关键测试用例 (New critical Test Cases to be added):**

| 接口名称 | 用例ID (建议编号) | 场景描述 | 前置条件 | 输入参数 | 预期结果 |
|---|---|---|---|---|---|
| API-GET-HomeMetadata | TC-API-Home-ERR-429 | 速率限制返回 | 服务端模拟 Rate Limit | 无 | 返回 429，JSON `{ code: 'rate_limit', requestId }`，前端展示非打扰提示 |
| API-GET-HomeMetadata | TC-API-Home-ERR-500 | 服务端错误返回 | 服务端抛出异常 | 无 | 返回 500，JSON `{ code: 'server_error', requestId }`，记录日志不含 PII |
| API-GET-HomeMetadata | TC-API-Home-Fields-BrandWelcome | 品牌与欢迎语字段校验 | 元信息服务正常 | 无 | `logoText` 与 `welcomeText` 均为非空字符串 |
| UI-HomePage | TC-UI-Route-Fallback | 非法路由统一回到首页并提示 | Router 可模拟跳转 | 路由 `/unknown` | 回退到 `/` 并出现提示“路由不存在”与“返回首页”按钮 |
| UI-HomePage | TC-UI-Keyboard-Order-Trigger | Tab 顺序与按键触发 | 渲染首页 | 键盘操作 | 焦点顺序严格为“登录注册→车票查询→订单管理→乘客管理→个人信息”，Enter/Space 触发点击 |
| UI-HomePage | TC-UI-Mobile-Layout-Size | 移动端触控尺寸 | 视口宽度 ≤768px | 无 | 按钮纵向堆叠，最小触控高度≥44px，文本不溢出可滚动 |
| UI-HomePage | TC-UI-TicketSearch-Form | 车票查询页面展示表单 | 跳转到 `/tickets/search` | 无 | 页面包含出发站、到达站与日期等查询表单控件 |
| UI-HomePage | TC-UI-Error-Logging-Sanitize | 错误日志脱敏校验 | 注入错误并捕获上报 | 错误对象 | 上报事件不包含姓名/手机号/证件号等，包含 `code` 与 `requestId` |

## 3. 最终验证摘要 (Final Validation Summary)

- 结论：根据对现有接口与测试的严格比对，当前需求覆盖率为 50%。计算口径：在 16 条验收标准中，后端 4 条覆盖充分（4/4），前端 12 条中仅 4 条覆盖充分（4/12），其余为缺失或不充分验证。`Home_Page/.artifacts/traceability_matrix.yml:107-108` 标注的 100% 覆盖与实际测试深度不一致。
- 下一步：建议据本报告更新接口文档与测试用例集，完善错误码与登录态接口，并补充无障碍与响应式的可验证断言。更新完成后，请重新生成需求-接口-测试的可追溯矩阵，并继续进行代码实现与验证。